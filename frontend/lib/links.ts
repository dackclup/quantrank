// Shared external resource URLs. Single source so the methodology link can't
// drift across the surfaces that point at it (the Sidebar resource rail, the
// compare-matrix group-header help affordances, the compare-view footnote).
// The methodology doc is a GitHub-hosted markdown file — the project has no
// in-app /methodology route (see #395 jargon-help shape), so every "what does
// this mean?" affordance points here.
export const METHODOLOGY_URL =
  'https://github.com/dackclup/quantrank/blob/main/docs/METHODOLOGY.md';

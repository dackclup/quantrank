name: Compute Monthly

on:
  # Phase-0 stub — the monthly `schedule:` is DISABLED (2026-06-22) so it
  # doesn't fire a no-op run every month. Re-enable the cron when the Phase-5
  # ML retrain actually lands here. Manual dispatch stays available.
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

permissions:
  contents: read

jobs:
  monthly:
    runs-on: ubuntu-latest
    steps:
      # No deps install — the Phase-0 stub only echoes. Restore
      # actions/setup-python + `pip install -e .` when the real retrain lands.
      - name: Monthly retrain (Phase 0 stub)
        run: echo "Phase 0 stub — ML retrain lands in Phase 5."
